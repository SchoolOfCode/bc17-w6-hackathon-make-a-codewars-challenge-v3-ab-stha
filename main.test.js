import{expect, test} from 'vitest';

test('Checking if vitest was installed successfully', () => {
});

test('Checking if function isHigher returns true when', () => {
    expect(isHigher(8,2).toBe(true));
});